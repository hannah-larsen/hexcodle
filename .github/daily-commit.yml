name: Daily Blank Commit

on:
  schedule:
    - cron: "0 0 * * *" # Runs every day at midnight UTC
  workflow_dispatch: # Allows manual triggering from the Actions tab

jobs:
  push-blank-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Create Blank Commit
        run: |
          git commit --allow-empty -m "chore: Daily trigger commit [skip ci]"

      - name: Push Changes
        uses: ad-m/github-push-action@v0.6.0
        with:
          github_token: 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
          branch: main
